services:
###########
### API ###
###########
  api:
    container_name: dbarkowsky-capstone-api
    tty: true
    restart: on-failure
    build:
      context: ./api
    environment:
      - PORT=${PORT:-3004}
      - HOSTNAME=${HOSTNAME:-localhost}
    networks:
      - purchase-net
    ports:
      - ${PORT:-3004}:3004
    healthcheck:
      test: curl localhost:${PORT:-3004}/api/health | grep 'API running'
      interval: 20s
      timeout: 30s
      retries: 10
      start_period: 120s
networks:
  purchase-net:
    driver: "bridge"
